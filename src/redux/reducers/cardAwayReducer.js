/* const initialState = {
	cardExists: false,
};

const reducer = (state = initialState, action) => {
	switch (action.type) {
		case "RESET_CARD_EXISTS":
			return {
				...state,
				cardExists: false,
			};
		default:
			return state;
	}
};

export default reducer;
 */
